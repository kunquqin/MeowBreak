---
description: main/settings.ts normalizeCategories 必须保留 SubReminder 各 mode 的全部字段
globs: ["src/main/settings.ts", "src/shared/settings.ts"]
---

# normalizeCategories 保全字段

- `main/settings.ts` 的 `normalizeCategories` 为每种 SubReminder mode 构造返回对象时，**必须包含该 mode 在 `shared/settings.ts` 中声明的全部字段**。
- 典型错误：给 stopwatch 加了 `content?: string` 但 normalization 仍只返回 `{ id, mode: 'stopwatch' }`，导致 auto-save → hydrate 后 content 丢失。
- **检查清单**（每次给 SubReminder 联合类型新增字段后）：
  1. `shared/settings.ts`：类型定义已加新字段
  2. `main/settings.ts` `normalizeCategories`：对应 mode 分支已保留新字段
  3. 渲染进程 auto-save → hydrate 后数据不丢失
