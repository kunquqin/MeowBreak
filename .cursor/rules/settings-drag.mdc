---
description: WorkBreak 设置页拖拽排序约定：大类与子项均用 @dnd-kit/sortable
globs: ["src/renderer/src/pages/Settings.tsx"]
---

# 设置页拖拽与排序

- **排序方案**：大类（`CategoryCard`）和子项（`SortableSubReminderItem`）均使用 **`@dnd-kit/sortable`**（`DndContext` + `SortableContext` + `useSortable`），手柄上挂 `listeners`。**不要用 Framer Motion `Reorder`**——Framer 的 FLIP 布局投影在动态高度变化时（秒表打点、内容展开）会导致兄弟卡片位置不更新、出现重叠。也不要用 HTML5 `draggable`/`onDragOver`/`onDrop`。
- **拖拽层级**：被拖拽的项必须始终在最上层。`CategoryCard` 用 `isChildDragging` + `isCatDragging` 在拖拽时提升整卡 `zIndex`（10000）；子项 `useSortable` 设 `zIndex: 99999`（isDragging 时）；子项列表容器加 `overflow-visible`，避免拖拽时被裁剪。
- **可变高度**：`useSortable` 的 `animateLayoutChanges` 设为 `() => false`，`transform` 样式只写 `translate3d(x,y,0)`（`sortableTranslateOnly`），**不写 scale**，避免矮/高卡片互拖时被拉伸或压扁。
